node_modules/
dist/
.DS_Store

# TypeScript incremental build cache (generated by tsc --noEmit)
*.tsbuildinfo

# vitest JSON report — the input to scripts/check-test-manifest.mjs. Regenerated by every
# `npm test`, which deletes it first so a stale report can never be read as a fresh run.
.vitest-report.json
